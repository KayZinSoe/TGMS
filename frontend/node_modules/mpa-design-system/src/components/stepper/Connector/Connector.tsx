import React from 'react';
import '../Stepper.scss';

const Connector: React.FC = () => {  

  return (
    <div id="mpa-ConnectorContainer" className={"mpa-stepper-connector-container"}>
      <span
        id="mpa-Connector"
        className={"mpa-stepper-connector"}
      ></span>
    </div>
  );
};

export default Connector;
