import * as React from "react";
import type { Options, Placement } from "@popperjs/core";
import "./Popper.scss";
export interface PopperProps {
    /**
     * Unique identifier of content instance, used for querying content element.
     */
    id: string;
    /**
     * Anchor element used to position the popper
     */
    anchorEl: HTMLElement;
    /**
     * Content to be displayed within the popper
     */
    children: React.ReactNode;
    /**
     * Popper.JS placement options.
     */
    placement?: Placement;
    /**
     * Popper.JS options.
     */
    popperProps?: Options;
}
declare const Popper: ({ id, anchorEl, children, placement, popperProps, }: PopperProps) => import("react/jsx-runtime").JSX.Element | null;
export default Popper;
