import * as React from "react";
import "./ModalTitle.scss";
interface ModalTitleProps {
    id: string;
    title?: string;
    thumbnail?: React.ReactNode;
    onClose?: () => void;
    showClose?: boolean;
}
declare const ModalTitle: ({ id, title, thumbnail, onClose, showClose, }: ModalTitleProps) => import("react/jsx-runtime").JSX.Element;
export default ModalTitle;
