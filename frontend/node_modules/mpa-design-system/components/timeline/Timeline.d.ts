import "./Timeline.scss";
import { TimelineInfoProps } from "./TimelineInfo/TimelineInfo";
export interface TimelineProps {
    /**
     * Array of timeline info to display
     */
    content: TimelineInfoProps[];
    /**
     * Toggle to show pagination
     * @default true
     */
    showPagination?: boolean;
    /**
     * Toggle to show vertical line on the left
     * @default true
     */
    showLine?: boolean;
    /**
     * Toggle to show x number of info per page
     * @default 2
     */
    pageRange?: number;
}
declare const Timeline: ({ content, showPagination, showLine, pageRange, }: TimelineProps) => import("react/jsx-runtime").JSX.Element;
export default Timeline;
